# Video de recompensa (opcional)

Si quieres usar un **archivo local** en lugar de los videos remotos (Mixkit), coloca aquí:

`reward.mp4`

El componente intenta primero URLs públicas de ejemplo y, si fallan, usa `/video/reward.mp4`.
