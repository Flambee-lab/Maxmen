/** Segundos por ronda (valor por defecto en intro y en GameScreen si no hay prop válida). */
export const DEFAULT_SECONDS_PER_ROUND = 30;

/** Por debajo de esto (segundos restantes) el reloj pasa a rojo y suena el tick. */
export const LOW_TIME_SECONDS_THRESHOLD = 10;
